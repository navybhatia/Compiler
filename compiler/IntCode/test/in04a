/* 
 * Tests: Empty if-then-else stmt
 */

    event open (string a1 , int a2 , unsigned int a3);
	bool condition;

    open(file, flags, mode) -> {
    if (condition);
	else;

    if (condition)
		if (condition) ;
		else;		

    if (condition)
		if (condition) ;
		else;
	else if (condition) ;
	else ;
    };
